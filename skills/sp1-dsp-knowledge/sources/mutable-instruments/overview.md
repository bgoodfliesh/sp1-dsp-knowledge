# Mutable Instruments

Catalog individual algorithms and modules here. Record the upstream revision,
license, hardware-specific assumptions, reusable concepts, and SP-1 adaptation
boundary. Do not mirror the entire upstream repository.

Use `../source-record-template.yaml` as the minimum metadata shape.
