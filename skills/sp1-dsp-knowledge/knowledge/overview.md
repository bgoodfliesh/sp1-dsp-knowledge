# Knowledge

This directory explains algorithms, architecture, real-time constraints,
numerical behavior, optimization, and validation. It is the bridge between
upstream research in `sources/` and code in `algorithms/` or `effects/`.
