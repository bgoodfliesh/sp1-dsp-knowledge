# Flanger effects

Add composed flanger implementations here. Keep reusable modulated-delay
primitives in `algorithms/delays/` and modulation utilities in
`algorithms/modulation/`.

Record delay range, feedback, modulation behavior, artifacts, and validation
status.
