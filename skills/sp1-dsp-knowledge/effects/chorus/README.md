# Chorus effects

Add composed chorus implementations here. Keep reusable delay and modulation
primitives in `algorithms/`.

Record modulation range, interpolation, voice count, latency, artifacts, and
effect-level validation.
