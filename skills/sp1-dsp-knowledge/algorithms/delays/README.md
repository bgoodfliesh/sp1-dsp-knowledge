# Delay algorithms

Add reusable delay-line primitives here, including fixed, fractional, and
modulated delays.

Each component should document interpolation, feedback bounds, buffer size,
latency, numerical format, validation status, and measured resource cost.
