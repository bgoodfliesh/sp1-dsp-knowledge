# Granular algorithms

Add reusable grain schedulers, windows, and granular-buffer primitives here.

Document scheduling bounds, window shape, memory use, density limits,
transient behavior, and validation status.
