# Tools

This directory contains reusable templates and metadata schemas. Tooling that
generates reports or validates records should preserve explicit evidence and
fail loudly on missing required fields.
